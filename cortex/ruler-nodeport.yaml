apiVersion: v1
kind: Service

metadata:
  name: ruler-nodeport
  namespace: cortex

spec:
  type: NodePort

  selector:
    app.kubernetes.io/component: ruler

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
